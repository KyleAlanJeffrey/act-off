{
  "name": "dub-off-repo",
  "private": true,
  "description": "Convenience scripts — the real apps live in app/ (the game) and site/ (the SEO landing page).",
  "scripts": {
    "dev": "npm --prefix app run dev",
    "dev:site": "npm --prefix site run dev",
    "deploy:app": "npm --prefix app run deploy",
    "deploy:site": "npm --prefix site run deploy"
  }
}
